/**
 * Get the validation rules for the resource.
 *
 * @return array
 */
public function rules(): array
{
    return [
        // Attributes
        {[ attributesRules|indentTabs(2) ]}

        // Relationships
        {[ relationshipsRules|indentTabs(2) ]}
    ];
}
